{
    "name": "tpwtypes",
    "version": "1.0.0",
    "main": "index.js",
    "type": "module",
    "description": "TypeScript definitions for TPW API",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "generate:types": "npx tsx ./src/generate_types.ts",
        "build": "npm run generate:types && tsc"
    },
    "devDependencies": {
        "@types/node": "^24.5.2"
    }
}
